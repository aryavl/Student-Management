"use client"
import React from 'react'
import { useDispatch, useSelector } from "react-redux";
import { addCurrentUser } from '@/redux/slices/userSlice';

const AddDispatchUser = () => {
  return (
    <div>AddDispatchUser</div>
  )
}

export default AddDispatchUser